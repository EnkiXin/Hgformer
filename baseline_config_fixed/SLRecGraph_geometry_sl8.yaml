# Apply after SLRecGraph_cd.yaml.
# Geometry-only SL(8) configuration used by the current Amazon-CD run.
embedding_size: 64
matrix_dim: 8
num_factors: 1
factor_aggregation: l2
n_layers: 0

init_std: 0.01
coord_clip: 0.75
reg_weight: 0.0
learning_rate: 0.003

schatten_p: 2
log_terms: 12
log_jitter: 0.0
symmetric_distance: false

score_scale: 1.0
learnable_score_scale: true
max_score_scale: 100.0
